# Puerto del servicio WebSocket
PORT=8003

# Mismo SECRET_KEY y ALGORITHM que el backend (para verificar el JWT de la cookie)
JWT_SECRET=tu_clave_secreta_muy_larga_aqui
JWT_ALGORITHM=HS256

# PostgreSQL (para validar la sesión única — active_session_id)
DB_HOST=localhost
DB_PORT=5432
DB_USER=ameth
DB_PASSWORD=Ameth2005
DB_NAME=sensor_db

# RabbitMQ (bus de eventos publicados por el backend de negocio)
RABBITMQ_URL=amqp://guest:guest@localhost:5672
WS_EXCHANGE=ws.events
